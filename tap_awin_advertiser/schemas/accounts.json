{
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "account_id": {
            "type": "integer",
            "description": "ID of the account, as known from the frontend URLs (e.g. https://ui.awin.com/awin/affiliate/45628)"
        },
        "account_name": {
            "type": ["null", "string"], 
            "description": "programme name for an advertiser and company name for a publisher"
        },
        "account_type": {
            "type": "string",
            "description": "can be either publisher or advertiser",
            "enum": ["publisher", "advertiser"]
        },
        "user_role": {
            "type": "string",
            "description": "the role your user account has for the account"
        }
    }
}